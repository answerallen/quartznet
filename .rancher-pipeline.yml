stages:
- name: build
  steps:
  - publishImageConfig:
      dockerfilePath: ./Dockerfile
      buildContext: .
      tag: r
      pushRemote: true
      registry: index.docker.io
timeout: 60
notification: {}
